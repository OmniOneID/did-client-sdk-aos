name: Android Instrumentation Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  android-test:
    runs-on: macos-latest
    timeout-minutes: 60  # 전체 작업 시간 제한 증가

    defaults:
      run:
        working-directory: source/did-client-sdk-aos

    steps:
    - uses: actions/checkout@v3

    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Android SDK
      uses: android-actions/setup-android@v3

    - name: Create and start emulator
      uses: reactivecircus/android-emulator-runner@v2
      with:
        api-level: 33  # Android 13
        arch: x86_64
        profile: pixel_6
        working-directory: source/did-client-sdk-aos
        script: |
          echo "Emulator is up and running"
          adb devices
          # 에뮬레이터 상태 확인
          adb shell getprop sys.boot_completed
          # ADB 서버 재시작
          adb kill-server
          adb start-server
          # Instrumentation 테스트 실행
          ./gradlew connectedAndroidTest
        emulator-options: |
          -no-snapshot-save
          -no-window
          -gpu swiftshader_indirect
          -noaudio
          -no-boot-anim
          -camera-back none
        disable-animations: true
        verbose: true
        force-avd-creation: false
        emulator-boot-timeout: 1800  # 30분으로 증가
        disk-size: 4000M

    - name: Upload test results
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: test-results
        path: source/did-client-sdk-aos/app/build/outputs/androidTest-results
